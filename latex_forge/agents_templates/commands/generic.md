This template defines its own commands and environments in its `.sty` / `.cls`
files and in `frontmatter/metadata.tex`. Open those files and look for
`\newcommand`, `\newenvironment`, `\NewDocumentCommand` and the document class
options to see what is available, rather than assuming a fixed set.