This is an installed template, so its exact layout and conventions are defined
by its own files rather than by latex-forge. Before editing, read the template's
sources — they are the source of truth for this document.

1. Open `@@NAME@@.tex` and follow its `\input{}` / `\include{}` order to see how
   the document is assembled and which files hold the content.
2. Put personal information (title, author, contact…) in
   `frontmatter/metadata.tex` if the template provides it.
3. Edit the template's own content files, keeping its existing structure, macros
   and environments.
4. If the template has a `bibliography/` folder or an `\addbibresource{}` /
   `\bibliography{}` line, add references there and cite them with `\cite{}`.

Replace every placeholder and guidance comment the template ships with real
content — the reader should never see leftover scaffolding.